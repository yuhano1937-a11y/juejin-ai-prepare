# 复制为 .env.local 并填入你自己的值（.env.local 已被 .gitignore 忽略，绝不进仓库）
# 采用 OpenAI 兼容协议：DeepSeek / 通义千问 / 智谱 GLM / Moonshot(Kimi) / 官方 OpenAI 都可
# 只需改 BASE_URL + MODEL 即可切换厂商

# 必填：你的 API Key，只在服务端读取，前端永远拿不到
# 远程厂商填真实 Key；本地 Ollama 不校验，填任意非空串即可（如 ollama）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 必填：服务地址（OpenAI 官方为 https://api.openai.com/v1）
# DeepSeek: https://api.deepseek.com/v1
# 通义千问: https://dashscope.aliyuncs.com/compatible-mode/v1
# 智谱 GLM: https://open.bigmodel.cn/api/paas/v4
# 本地 Ollama: http://localhost:11434/v1   ← 跑通大模型不花一分钱
OPENAI_BASE_URL=https://api.openai.com/v1

# 选填：模型名
OPENAI_MODEL=gpt-4o-mini
